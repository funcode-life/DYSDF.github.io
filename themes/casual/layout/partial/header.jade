header.site-header
  figure.site-banner(style={'background-image': 'url(' + url_for(theme.banner) + ')'})
    h3.site-title
      a(href=url_for() data-text=config.title)
        != config.title
  include search
